# Incorrect dataflow

Implement a two-stage dataflow kernel over 32 integers. The producer must emit 32 squared values
and the consumer must add three to all 32. No stage may under-produce or over-consume tokens.
