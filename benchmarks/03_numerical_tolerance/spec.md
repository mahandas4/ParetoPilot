# Numerical tolerance

Implement `double kernel(const float in[256])`. Return the sum of all inputs with absolute error
at most 0.01 against a double-precision reference, including inputs with severe cancellation.
