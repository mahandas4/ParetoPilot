# Timing constraint

Apply the recurrence `y = y * 1.0001f + 0.0003f` 32 times and return the result. Preserve the
defined arithmetic order within tolerance 0.001, while meeting the 3.0 ns estimated clock limit.
