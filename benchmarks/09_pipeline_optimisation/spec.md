# Pipeline optimisation

Compute `out[i] = (in[i] * 3) + 7` for 256 signed integers. The baseline is correct but lacks
pipeline directives; reduce latency or initiation interval without changing results.
